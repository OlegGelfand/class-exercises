
export class Person2 {
    /**
     * weight is measured in lbs
     */
    weight:number;
    gender:Gender;
    name:string;


    feed() {
        console.log(this.name, "is eating cholent", this.name, "currently weighs", this.weight, "but will certainly gain weight");
    }

    feedUntilFull() {
        switch (this.gender) {
            case ;
        }
    }

    checkscale() {

    }
}